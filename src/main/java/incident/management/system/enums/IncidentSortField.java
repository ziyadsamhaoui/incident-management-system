package incident.management.system.enums;

public enum IncidentSortField {
    DECLARED_AT,
    IN_PROGRESS_AT,
    RESOLVED_AT,
    ASSIGNED_AT,
    CLOSED_AT,
    PRIORITY,
    STATUS,
    REFERENCE
}
